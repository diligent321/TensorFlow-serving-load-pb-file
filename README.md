# TensorFlow-serving-load-pb-file
In order to easily switch between several pretrained model, the cpu server is used to load a freezed pb file(the process of weights freeze can be made on a standalone gpu server), and start the tensorflow serving.
